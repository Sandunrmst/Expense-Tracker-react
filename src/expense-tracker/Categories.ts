//Define for reusable category list
const categorries = ["Groceries", "Utilities", "Entertainment"] as const;
//At the end "as const" is use for set this categorries for read only

export default categorries